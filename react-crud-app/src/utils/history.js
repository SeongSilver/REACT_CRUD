import { createBrowserHistory } from 'history';

/* 
    history 패키지는 임의의 환경(브라우저 포함)에서 구동되는 
    JavaScript 애플리케이션에서 세션 히스토리(방문 기록)의 관리와 
    내비게이션 등을 쉽게 할 수 있도록 도와주는 라이브러리이다
*/
export default createBrowserHistory();
